///
/// HybridNoahToolsSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HttpResponse` to properly resolve imports.
namespace margelo::nitro::noahtools { struct HttpResponse; }
// Forward declaration of `UnifiedPushDistributor` to properly resolve imports.
namespace margelo::nitro::noahtools { struct UnifiedPushDistributor; }

#include <string>
#include <vector>
#include <NitroModules/Promise.hpp>
#include "HttpResponse.hpp"
#include <unordered_map>
#include "UnifiedPushDistributor.hpp"
#include <NitroModules/Null.hpp>
#include <variant>
#include <optional>

namespace margelo::nitro::noahtools {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NoahTools`
   * Inherit this class to create instances of `HybridNoahToolsSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNoahTools: public HybridNoahToolsSpec {
   * public:
   *   HybridNoahTools(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNoahToolsSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNoahToolsSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNoahToolsSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string getAppVariant() = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> getAppLogs() = 0;
      virtual std::shared_ptr<Promise<std::string>> createBackup(const std::string& mnemonic) = 0;
      virtual std::shared_ptr<Promise<bool>> restoreBackup(const std::string& encryptedData, const std::string& mnemonic) = 0;
      virtual std::shared_ptr<Promise<HttpResponse>> nativePost(const std::string& url, const std::string& body, const std::unordered_map<std::string, std::string>& headers, double timeoutSeconds) = 0;
      virtual std::shared_ptr<Promise<HttpResponse>> nativeGet(const std::string& url, const std::unordered_map<std::string, std::string>& headers, double timeoutSeconds) = 0;
      virtual void nativeLog(const std::string& level, const std::string& tag, const std::string& message) = 0;
      virtual std::shared_ptr<Promise<void>> playAudio(const std::string& filePath) = 0;
      virtual void pauseAudio() = 0;
      virtual void stopAudio() = 0;
      virtual void resumeAudio() = 0;
      virtual void seekAudio(double positionSeconds) = 0;
      virtual double getAudioDuration() = 0;
      virtual double getAudioPosition() = 0;
      virtual bool isAudioPlaying() = 0;
      virtual void updateWidgetData(double totalBalance, double onchainBalance, double offchainBalance, double pendingBalance, double closestExpiryBlocks, double expiryThreshold, const std::string& appGroup) = 0;
      virtual bool isGooglePlayServicesAvailable() = 0;
      virtual void registerUnifiedPush() = 0;
      virtual std::string getUnifiedPushEndpoint() = 0;
      virtual std::vector<UnifiedPushDistributor> getUnifiedPushDistributors() = 0;
      virtual void setUnifiedPushDistributor(const std::optional<std::variant<nitro::NullType, std::string>>& distributorId) = 0;
      virtual std::shared_ptr<Promise<void>> storeNativeMnemonic(const std::string& mnemonic) = 0;
      virtual std::shared_ptr<Promise<void>> storeNativeServerAccessToken(const std::string& token) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NoahTools";
  };

} // namespace margelo::nitro::noahtools
